import type { Showcase } from "@/lib/showcase";

/**
 * A published snapshot of one person's run through the roadmap, shown on the
 * live site as a worked example. Generate it with the showcase builder under
 * "Your business" → Publish a worked example, then paste the result here.
 *
 * It carries no notes and no unapproved fields — see src/lib/showcase.ts.
 *
 * Forks: leave this as `null` (or replace it with your own run). It is only
 * ever an example view and never becomes a visitor's starting state.
 */
export const SHOWCASE: Showcase | null = null;
